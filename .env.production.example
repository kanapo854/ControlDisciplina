# ============================================
# BACKEND - Variables de Entorno (Render)
# ============================================

# Entorno
NODE_ENV=production

# Puerto (Render asigna automáticamente)
PORT=10000

# Base de Datos PostgreSQL
# IMPORTANTE: Usar la DATABASE_URL interna de Render
DB_HOST=your-render-postgres-host.render.com
DB_PORT=5432
DB_NAME=control_disciplina
DB_USER=admin_disciplina
DB_PASSWORD=your_secure_database_password_here

# JWT (JSON Web Token)
# IMPORTANTE: Generar un secret único y seguro
# Ejemplo: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_recommended_64
JWT_EXPIRE=7d

# Bcrypt
BCRYPT_ROUNDS=12

# MFA (Multi-Factor Authentication)
# IMPORTANTE: Generar un secret único para OTP
MFA_SECRET=your_mfa_secret_key_minimum_32_characters

# Email (SMTP) - Para envío de códigos MFA
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your_app_password_here
EMAIL_FROM=Control Disciplina <noreply@controldisciplina.com>

# Frontend URL (Vercel)
FRONTEND_URL=https://your-app.vercel.app

# SSL/TLS (En Render usar false, ellos manejan HTTPS)
SSL_ENABLED=false

# CORS
CORS_ORIGIN=https://your-app.vercel.app

# ============================================
# FRONTEND - Variables de Entorno (Vercel)
# ============================================

# API Backend URL (Render)
REACT_APP_API_URL=https://your-backend.onrender.com

# ============================================
# NOTAS DE SEGURIDAD
# ============================================
# 1. JWT_SECRET: Usar mínimo 32 caracteres, recomendado 64
#    Generar: openssl rand -base64 64
#
# 2. DB_PASSWORD: Contraseña fuerte, mínimo 16 caracteres
#    Incluir: mayúsculas, minúsculas, números, símbolos
#
# 3. EMAIL_PASSWORD: Para Gmail usar "App Password"
#    No usar la contraseña real de Gmail
#    Crear en: https://myaccount.google.com/apppasswords
#
# 4. MFA_SECRET: Secret único para generación de OTP
#    Generar: openssl rand -base64 32
#
# 5. CORS_ORIGIN: Debe coincidir exactamente con la URL de Vercel
#
# 6. FRONTEND_URL: URL completa de Vercel (sin trailing slash)
#
# 7. Render Database: Usar la "Internal Database URL"
#    No la External (evita cargos por tráfico externo)
